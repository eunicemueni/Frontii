{
  "name": "skillmatch",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build needed — plain React via CDN'"
  },
  "dependencies": {
    "axios": "^1.6.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14"
  }
}
